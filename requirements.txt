# Unlimited-OCR: ML Stack Only
# Python 3.11 + CUDA 12.1 + RTX 4060 (8GB VRAM)
#
# IMPORTANT: For GPU support (CUDA 12.1), install with special index:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# For CPU-only (not recommended), use normal pip:
#   pip install -r requirements.txt

# Core ML Stack
torch>=2.0.0
torchvision>=0.15.0
transformers==4.57.1
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Baidu Unlimited-OCR model dependencies
numpy>=1.24.0
Pillow>=11.0.0
einops>=0.7.0
addict>=2.4.0
easydict>=1.9
matplotlib>=3.5.0
pymupdf>=1.24.0
psutil>=7.2.0

# Optional: for development/testing
python-dotenv>=1.0.0
